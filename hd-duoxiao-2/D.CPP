#include<bits/stdc++.h>
#define read(x) scanf("%d",&x)
using namespace std;

int main() {
    int n;
    while(read(n) != EOF) {
        if(n==3) {
            cout<<"No"<<endl;
        }
        else cout<<"Yes"<<endl;
    }
}