1.输入的时候:特别是一行有很多数字那种,注意下是否第一个是写的有多少个数字
2.注意while内的 ~scanf 前面的符号不要缺少
3.整数的时候尽量使用int 超了就long long ,double尽量只用在小数里
4.文字类的输出尽量复制粘贴题中的内容
5.推论公式的时候分母可能为0
6.日期中的2月29日要特别注意==时间长度/生日/

7.struct 数组形式.非常好用
	struct people{

	};
	struct people aName[10];

8.开方尽量先存到一个变量中,再使用,尽量减少一些重复运算
9.开方的时候 开方结果的左侧数字量更少 特别是在做约数题的时候减少很多判断
10.n进制对n-1可以整除方法是各位数字之和为n-1的倍数
11.计算机博弈-1:
	轮流报数:n/(m+1)
12.d+f => 数据流从f到d会有一个变量类型变换

13. inline void name()
	inline 内联函数,为了解决一些频繁调用的小函数_会大量消耗栈空间
	使用的时候内部不能包含复杂的while.switch.并且不能直接调用递归函数
14.pow操作 并加上了取模操作
	while(x)
	{
		if (x & 1)
		{
			ret = ret * e % m;//二进制判断x末尾是不是1，然后进行赋值
		}
		e = e * e % m;//每次循环都要重新更新相乘的倍数
		x >>= 1;//除2，以幂次递减
	}//2*2*4*16*64.....
15.printf(a[i]) 打印a[i]这行，注意会遇到0时候结束，a是一个二维字符数组。
16.注意输入一个范围时候，要记得判断大小，和交换

健忘



