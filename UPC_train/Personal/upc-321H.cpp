#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
	priority_queue <int a[]> q;
	int n;
	struct point p[1005];
	cin >> n;
	int max=0;
	int pos;
	int book[505]={0};
	for (int i = 1; i <= n; ++i)
	{
		for (int k = 0; k < i; ++k)
		{
			p[i].
		}
		for (int j = i+1; j <= n; ++j)
		{
			cin >> p[i].val;
			if (p[i].val > max)
			{
				pos = i;
			}
			p[i].x = i;
			p[i].y = j;
		}
	}
	for (int i = 0; i < count; ++i)
	{
		q.insert(p[pos]);
	}
	// 
// unfinished
//

	return 0;
}