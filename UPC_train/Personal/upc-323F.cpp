#include <bits/stdc++.h>
using namespace std;
struct node
{
	int val;
	node *next;
};
// bool cmp(node a,node b){return a.a>b.a;}
// int a[200004];
struct node lst[200004];
int main(int argc, char const *argv[])
{
	int n;
	int a[200004];
	for (int i = 1; i < n; ++i)
	{
		node *p;
		p = 
		p.next = lst[i].next;
		for (int j = 0; j < j; ++j)
		{		
			cin >> p.val >> p.next->val;
			p = p.next;
		}
	}
	// for (int i = 1; i < n; ++i)
	// {
	// 	cin >> a[i];
	// }
	// sort(lst+1,lst+n,cmp);
	// int maxnum=0;
	// for (int i = 1; i < n; ++i)
	// {
	// 	int y = lst[i].b;
	// 	for (int j = 0; j < n; ++j)
	// 	{
			
	// 	}

	// }
	return 0;
}